# بوت التداول السعودي - استراتيجية Grid Trading

بوت تداول آلي للسوق السعودي يعتمد على **استراتيجية الشبكة (Grid Trading)** مع **التحليل الفني العميق** لتحقيق أرباح مستدامة من تذبذبات الأسعار.

## نظرة عامة

يستخدم هذا البوت استراتيجية Grid Trading التي تستفيد من تحركات السوق في كلا الاتجاهين (صعوداً وهبوطاً) من خلال وضع أوامر شراء وبيع على مستويات أسعار محددة مسبقاً. يتم دمج هذه الاستراتيجية مع مجموعة من المؤشرات الفنية المتقدمة لفلترة الإشارات وتحسين معدل النجاح.

## الميزات الرئيسية

### 1. استراتيجية الشبكة الديناميكية

البوت يستخدم استراتيجية شبكة ديناميكية تتكيف مع تقلبات السوق بدلاً من الاعتماد على معايير ثابتة. يتم حساب معايير الشبكة بناءً على مؤشر **Average True Range (ATR)** الذي يقيس التقلبات الحالية للسهم.

**معاير الشبكة:**
- **السعر المركزي**: يتم حسابه باستخدام المتوسط المتحرك الأسي (EMA 20)
- **المسافة بين المستويات**: 50% من قيمة ATR
- **عدد المستويات**: 5 مستويات أعلى وأسفل السعر المركزي (إجمالي 11 مستوى)

### 2. التحليل الفني العميق

يستخدم البوت مجموعة من المؤشرات الفنية لفلترة الإشارات وتجنب الدخول في صفقات ذات احتمالية نجاح منخفضة:

#### المؤشرات المستخدمة:

| المؤشر | الغرض | المعايير |
|--------|-------|---------|
| **RSI** (Relative Strength Index) | قياس قوة الزخم وتجنب التشبع | يجب أن يكون بين 35 و 65 |
| **MACD** (Moving Average Convergence Divergence) | تحديد الاتجاه والزخم | يجب أن يكون قريباً من خط الإشارة |
| **Bollinger Bands** | قياس التقلبات ونطاق السعر | السعر يجب أن يكون ضمن النطاق |
| **EMA 20** | تحديد السعر المركزي للشبكة | أساس حساب مستويات الشبكة |
| **ATR** | قياس التقلبات | أساس حساب المسافة بين المستويات |
| **Volume MA** | تحليل حجم التداول | التأكد من وجود سيولة كافية |

#### شروط فلترة الإشارات:

لكي يتم قبول إشارة التداول، يجب أن تتحقق جميع الشروط التالية:

1. **RSI بين 35 و 65**: تجنب الدخول في مناطق التشبع الشرائي أو البيعي
2. **السعر ضمن نطاق Bollinger Bands**: التأكد من أن السعر لم يخرج عن النطاق الطبيعي
3. **MACD قريب من خط الإشارة**: التأكد من عدم وجود تباعد كبير يدل على اتجاه قوي

### 3. إدارة المخاطر التلقائية

كل صفقة تتضمن:
- **وقف الخسارة (Stop Loss)**: 1.5 × المسافة بين مستويات الشبكة
- **جني الربح (Take Profit)**: 1.0 × المسافة بين مستويات الشبكة

## بنية المشروع

```
saudi_trading_bot/
├── data_fetcher.py       # جلب بيانات الأسهم من Yahoo Finance
├── deep_analyzer.py      # حساب المؤشرات الفنية والتحليل
├── grid_strategy.py      # تنفيذ استراتيجية الشبكة وإدارة الأوامر
├── trading_bot.py        # البوت الرئيسي الذي يدمج جميع الوحدات
├── backtest_results.json # نتائج الاختبار الخلفي
└── README.md            # هذا الملف
```

## المتطلبات

```bash
pip install yfinance pandas numpy
```

## طريقة الاستخدام

### 1. الاستخدام الأساسي

```python
from trading_bot import TradingBot

# إنشاء البوت لسهم أرامكو
bot = TradingBot(
    symbol='2222.SR',      # رمز السهم
    interval='5m',         # الفترة الزمنية (5 دقائق)
    period='30d',          # المدة الزمنية (30 يوم)
    position_size=100      # حجم الصفقة (عدد الأسهم)
)

# تهيئة البوت
if bot.initialize():
    # تنفيذ الاختبار الخلفي
    results = bot.backtest()
    
    # حفظ النتائج
    bot.save_results('backtest_results.json')
```

### 2. تخصيص المعايير

```python
# يمكنك تغيير المعايير حسب احتياجك
bot = TradingBot(
    symbol='1120.SR',      # سهم الراجحي
    interval='15m',        # 15 دقيقة
    period='3mo',          # 3 أشهر
    position_size=50       # 50 سهم
)
```

### 3. الأسهم المدعومة

البوت يدعم جميع أسهم السوق السعودي المتاحة على Yahoo Finance. يجب إضافة `.SR` بعد رمز السهم:

| السهم | الرمز |
|------|------|
| أرامكو | 2222.SR |
| الراجحي | 1120.SR |
| سابك | 2010.SR |
| الأهلي | 1180.SR |
| STC | 7010.SR |

## نتائج الاختبار الخلفي

تم اختبار البوت على سهم أرامكو (2222.SR) باستخدام بيانات 30 يوم بفترة 5 دقائق:

### النتائج:

| المؤشر | القيمة |
|--------|--------|
| إجمالي الصفقات | 26 صفقة |
| الصفقات الرابحة | 10 صفقات |
| الصفقات الخاسرة | 5 صفقات |
| معدل الربح | 38.46% |
| إجمالي الربح/الخسارة | +8.00 ريال |
| متوسط الربح لكل صفقة | +0.31 ريال |

### تحليل النتائج:

على الرغم من أن معدل الربح (38.46%) قد يبدو منخفضاً، إلا أن الاستراتيجية حققت ربحاً إجمالياً إيجابياً. هذا يعود إلى أن الصفقات الرابحة حققت أرباحاً أكبر من خسائر الصفقات الخاسرة، وهو ما يُعرف بـ **Risk-Reward Ratio** الإيجابي.

**ملاحظات مهمة:**
- الاختبار تم على فترة قصيرة (30 يوم) وبفترة زمنية صغيرة (5 دقائق)
- النتائج قد تختلف باختلاف السهم والفترة الزمنية
- يُنصح باختبار البوت على فترات زمنية أطول قبل الاستخدام الفعلي

## كيف يعمل البوت؟

### 1. جلب البيانات

يستخدم البوت مكتبة `yfinance` لجلب بيانات الأسعار التاريخية من Yahoo Finance API. يتم جلب البيانات بالفترة الزمنية المحددة (مثلاً 5 دقائق) للمدة المطلوبة (مثلاً 30 يوم).

```python
# مثال على البيانات المجلوبة
#                         open    high     low   close    volume
# datetime                                                      
# 2025-11-09 11:40:00  25.72  25.74  25.70  25.72  1234567
# 2025-11-09 11:45:00  25.70  25.73  25.68  25.70   987654
```

### 2. حساب المؤشرات

بعد جلب البيانات، يقوم البوت بحساب جميع المؤشرات الفنية المطلوبة:

```python
# حساب المؤشرات
df_with_indicators = analyzer.calculate_indicators(df)

# النتيجة: بيانات مع المؤشرات المضافة
#                         close    rsi   macd  bb_upper  bb_lower
# datetime                                                       
# 2025-11-09 11:40:00  25.72  58.33  0.018     25.73     25.64
# 2025-11-09 11:45:00  25.70  53.85  0.018     25.73     25.64
```

### 3. إنشاء الشبكة

يتم حساب معايير الشبكة الديناميكية بناءً على ATR و EMA:

```python
# معايير الشبكة
mid_price = 25.69      # EMA 20
grid_distance = 0.02   # 50% من ATR
num_levels = 5         # عدد المستويات

# مستويات الشبكة الناتجة:
# 25.79, 25.77, 25.75, 25.73, 25.71, 25.69, 25.67, 25.65, 25.63, 25.61, 25.59
```

### 4. مراقبة التقاطعات

يراقب البوت حركة السعر ويكتشف التقاطعات مع مستويات الشبكة:

```python
# إذا تقاطع السعر مع مستوى 25.75
signal = strategy.generate_signal(current_price=25.75, previous_price=25.73)

# يتم توليد إشارة تداول
# signal = {
#     'price': 25.75,
#     'grid_level': 25.75,
#     'stop_loss_distance': 0.03,
#     'take_profit_distance': 0.02
# }
```

### 5. فلترة الإشارات

قبل فتح الصفقة، يتم التحقق من شروط التحليل العميق:

```python
validity = analyzer.check_signal_validity(df_with_indicators)

# إذا كانت الإشارة صالحة:
# validity = {
#     'valid': True,
#     'reasons': [
#         '✓ RSI في النطاق المقبول: 50.00',
#         '✓ السعر ضمن نطاق Bollinger Bands',
#         '✓ MACD قريب من خط الإشارة'
#     ]
# }
```

### 6. فتح الصفقات

عند كل تقاطع صالح، يتم فتح صفقتين:
- **صفقة شراء (Buy)**: للاستفادة من الارتفاع
- **صفقة بيع (Sell)**: للاستفادة من الانخفاض

```python
# صفقة شراء
buy_order = Order(
    order_type='buy',
    entry_price=25.75,
    stop_loss=25.72,      # 25.75 - 0.03
    take_profit=25.77     # 25.75 + 0.02
)

# صفقة بيع
sell_order = Order(
    order_type='sell',
    entry_price=25.75,
    stop_loss=25.78,      # 25.75 + 0.03
    take_profit=25.73     # 25.75 - 0.02
)
```

### 7. إدارة الصفقات

يراقب البوت جميع الصفقات المفتوحة ويغلقها تلقائياً عند:
- الوصول إلى وقف الخسارة
- الوصول إلى جني الربح

```python
# في كل تحديث للسعر
for order in open_orders:
    if order.check_exit(current_price):
        # تم إغلاق الصفقة
        print(f"تم إغلاق الصفقة #{order.order_id}")
        print(f"الربح/الخسارة: {order.pnl:.2f}")
```

## التحسينات الممكنة

### 1. تحسين معايير الفلترة

يمكن تحسين شروط فلترة الإشارات لزيادة معدل النجاح:

```python
# في deep_analyzer.py - دالة check_signal_validity
def check_signal_validity(self, df, index=-1):
    # إضافة شروط إضافية:
    
    # شرط 4: حجم التداول أعلى من المتوسط
    if df['volume'].iloc[index] < df['volume_ma'].iloc[index]:
        result['valid'] = False
        result['reasons'].append("حجم التداول أقل من المتوسط")
    
    # شرط 5: الاتجاه العام إيجابي
    if df['ema_20'].iloc[index] < df['ema_20'].iloc[index-5]:
        result['valid'] = False
        result['reasons'].append("الاتجاه العام سلبي")
```

### 2. تعديل نسبة المخاطرة/العائد

يمكن تعديل نسبة وقف الخسارة وجني الربح لتحسين النتائج:

```python
# في grid_strategy.py - دالة generate_signal
signal = {
    'stop_loss_distance': 2.0 * self.grid_distance,  # زيادة وقف الخسارة
    'take_profit_distance': 1.5 * self.grid_distance  # زيادة جني الربح
}
```

### 3. إضافة أنواع أوامر متقدمة

يمكن إضافة أنواع أوامر أخرى مثل:
- **Trailing Stop Loss**: وقف خسارة متحرك يتبع السعر
- **Partial Take Profit**: جني ربح جزئي عند مستويات محددة
- **Time-based Exit**: إغلاق الصفقة بعد فترة زمنية محددة

### 4. التكامل مع منصات التداول

لاستخدام البوت في التداول الفعلي، يجب التكامل مع منصة تداول تدعم API:

```python
# مثال على التكامل (يتطلب حساب وAPI key)
from trading_platform import TradingClient

client = TradingClient(api_key='YOUR_API_KEY')

# تنفيذ أمر شراء فعلي
order = client.place_order(
    symbol='2222.SR',
    side='buy',
    quantity=100,
    order_type='limit',
    price=25.75
)
```

## التحذيرات والملاحظات المهمة

### ⚠️ تحذيرات

1. **هذا البوت للأغراض التعليمية والاختبار فقط**
   - لا يُنصح باستخدامه في التداول الفعلي بدون اختبار شامل
   - النتائج السابقة لا تضمن النتائج المستقبلية

2. **مخاطر التداول**
   - التداول في الأسواق المالية ينطوي على مخاطر عالية
   - قد تخسر رأس المال المستثمر بالكامل
   - استشر مستشاراً مالياً قبل اتخاذ أي قرارات استثمارية

3. **قيود البيانات**
   - البيانات المجانية من Yahoo Finance قد تحتوي على تأخير
   - قد تكون البيانات غير دقيقة أو غير كاملة
   - للتداول الفعلي، استخدم مصادر بيانات احترافية

4. **قيود الاستراتيجية**
   - استراتيجية Grid Trading تعمل بشكل أفضل في الأسواق المتذبذبة
   - قد تؤدي إلى خسائر في الأسواق ذات الاتجاه القوي
   - تتطلب رأس مال كافٍ لتحمل التقلبات

### 📝 ملاحظات

1. **الفترة الزمنية المناسبة**
   - للتداول اليومي: استخدم فترات 5m أو 15m
   - للتداول المتأرجح: استخدم فترات 1h أو 4h
   - للاستثمار طويل الأجل: استخدم فترات 1d أو 1wk

2. **اختيار الأسهم**
   - اختر أسهماً ذات سيولة عالية
   - تجنب الأسهم ذات التقلبات الشديدة
   - راجع الأخبار والتقارير المالية قبل التداول

3. **إدارة رأس المال**
   - لا تستثمر أكثر من 2-5% من رأس المال في صفقة واحدة
   - احتفظ باحتياطي نقدي للفرص الجديدة
   - نوّع استثماراتك عبر أسهم مختلفة

## الدعم والمساهمة

### الإبلاغ عن المشاكل

إذا واجهت أي مشاكل أو أخطاء، يرجى:
1. التأكد من تثبيت جميع المتطلبات
2. التحقق من صحة رمز السهم
3. مراجعة رسائل الخطأ في السجلات

### المساهمة

المساهمات مرحب بها! يمكنك المساهمة من خلال:
- إضافة ميزات جديدة
- تحسين الاستراتيجيات الموجودة
- إصلاح الأخطاء
- تحسين التوثيق

## الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام التعليمي والبحثي.

## إخلاء المسؤولية

المعلومات والأدوات المقدمة في هذا المشروع هي لأغراض تعليمية فقط ولا تشكل نصيحة استثمارية. المطور غير مسؤول عن أي خسائر مالية قد تنتج عن استخدام هذا البوت. يجب عليك إجراء بحثك الخاص واستشارة مستشار مالي مؤهل قبل اتخاذ أي قرارات استثمارية.

---

**تم التطوير بواسطة**: Manus AI  
**التاريخ**: نوفمبر 2025  
**الإصدار**: 1.0.0
